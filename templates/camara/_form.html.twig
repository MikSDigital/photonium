{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="ui primary button">{{ button_label|default('Guardar') }}</button>
    <a class="ui button" href="{{ path('camara_index') }}">Volver</a>
{{ form_end(form) }}