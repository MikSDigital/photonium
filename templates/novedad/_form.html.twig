{% extends 'base.html.twig' %}

{% block body %}
<h1 class="">Novedad</h1>

<div class="ui raised segment">
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="ui primary button" formnovalidate>
		{{ button_label|default('Guardar') }}
	</button>
	<a class="ui button" href="{{ path('novedad_index') }}">Volver</a>
	{{ form_end(form) }}
</div>

{% endblock %}
